{
  "name": "web_dev",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build": "npm run build && gh-pages -d dist",
    "deploy": "npm run build && gh-pages -d dist && concurrently \"npm run start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^8.2.1",
    "gh-pages": "^6.0.0"
  }
}
